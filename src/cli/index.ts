#!/usr/bin/env node
// CLI entry point — fully implemented in issue #36
// This stub exists so tsup can resolve the entry point.
console.error('logilog CLI not yet implemented. See issue #36.')
process.exit(1)
